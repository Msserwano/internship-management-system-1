PORT=5000
NODE_ENV=development
JWT_SECRET=kcca_internship_jwt_secret_2026_uganda
DATABASE_URL=postgres://postgres:1234@localhost:5432/kcca_ims
SKIP_EMAIL_VERIFICATION=true

# Email Verification (Gmail SMTP)
# Replace EMAIL_USER and EMAIL_PASS with your Gmail & Google App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
EMAIL_USER=sserwanomoris@gmail.com
EMAIL_PASS=jixt uady rzoe dnyl
EMAIL_FROM="KCCA Internship Portal <sserwanomoris@gmail.com>"
# Secondary sender (optional) — not used automatically by nodemailer but kept for reference
SECONDARY_EMAIL=morissserwano@gmail.com
# Frontend URL (used to build links in notification emails)
FRONTEND_URL=http://localhost:5173

# Alias: some systems use EMAIL_PASSWORD instead of EMAIL_PASS
EMAIL_PASSWORD=${EMAIL_PASS}
